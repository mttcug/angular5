import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CollectCompleteComponent } from './collect-complete.component';

describe('CollectCompleteComponent', () => {
  let component: CollectCompleteComponent;
  let fixture: ComponentFixture<CollectCompleteComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CollectCompleteComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CollectCompleteComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
