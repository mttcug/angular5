import { TestBed, inject } from '@angular/core/testing';

import { JsonrpcRequestModelService } from './jsonrpc-request-model.service';

describe('JsonrpcRequestModelService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [JsonrpcRequestModelService]
    });
  });

  it('should be created', inject([JsonrpcRequestModelService], (service: JsonrpcRequestModelService) => {
    expect(service).toBeTruthy();
  }));
});
