import { TestBed, inject } from '@angular/core/testing';

import { PeripheralDataService } from './peripheral-data.service';

describe('PeripheralDataService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PeripheralDataService]
    });
  });

  it('should be created', inject([PeripheralDataService], (service: PeripheralDataService) => {
    expect(service).toBeTruthy();
  }));
});
