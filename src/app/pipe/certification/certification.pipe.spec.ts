import { CertificationPipe } from './certification.pipe';

describe('CertificationPipe', () => {
  it('create an instance', () => {
    const pipe = new CertificationPipe();
    expect(pipe).toBeTruthy();
  });
});
