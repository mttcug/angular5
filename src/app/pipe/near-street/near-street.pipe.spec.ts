import { NearStreetPipe } from './near-street.pipe';

describe('NearStreetPipe', () => {
  it('create an instance', () => {
    const pipe = new NearStreetPipe();
    expect(pipe).toBeTruthy();
  });
});
